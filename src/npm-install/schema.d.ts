export interface Schema {
  /**
   * Stringified array of dependencies to be installed
   */
  dependencies: string;
  
  /**
   * Stringified array of dependencies to be installed
   */
  devDependencies: string;
}
